module Base {

    sound cardFlip {
        category = GameNight, is3D = true,
        clip { file = common/media/sound/cardFlip.ogg, } }

    sound pieceMove {
        category = GameNight, is3D = true,
        clip { file = common/media/sound/pieceMove.ogg, } }

    sound dieRoll {
        category = GameNight, is3D = true,
        clip { file = common/media/sound/dieRoll.ogg, } }
}