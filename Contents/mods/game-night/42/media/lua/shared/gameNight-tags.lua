local tags = GAME_NIGHT_TAGS -- {}

-- Automatically applied to all game pieces, boards, and boxes so the board window can identify them.
---tags.GAME_NIGHT = ItemTag.register("gamenight:GAME_NIGHT")
---function tags.register(tag) tags[tag] = ItemTag.register("gamenight:"..tag) end

return tags
